"use strict";
//? In TS, an array is a data type that can store multiple values of diff data types sequentially.
//TI(type inference) and TA(type annotation)
//! Using square brackets:
const numbs = [1, 5, 7, 9, 43]; //? type is array of number
//! Using the Array constructor:
const nums1 = new Array(1, 5, 7, 2, 8);
//! Using the Array.of method
//const names = Array.of("Deep", "Kalki", "D_Kumar")
//todo Accessing elements using index:
console.log(numbs[2]);
//console.log(Array.of("Deep", "Kalki", "D_Kumar")) 
